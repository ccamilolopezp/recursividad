name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        run: mvn -B package --file pom.xml    

      - name: Run unit tests
        run: mvn -B test --file pom.xml

      - name: Run main class
        run: mvn -B exec:java -Dexec.mainClass="com.ejemplo.App" --file pom.xml

      # Optional: upload dependency graph to GitHub (Dependabot alerts)
      # Uncomment if you want security/dependency tracking
      # - name: Update dependency graph
      #   uses: advanced-security/maven-dependency-submission-action@v4
